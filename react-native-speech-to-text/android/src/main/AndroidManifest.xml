<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
      Android 11+ package visibility: without this the app cannot see any
      RecognitionService, and SpeechRecognizer.isRecognitionAvailable() returns
      false even when a recognizer is installed.
    -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

</manifest>
